apiVersion: v1
kind: PersistentVolume
metadata:
  name: mariadb-volume
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 3Gi
  accessModes:
    - ReadWriteOncePod
  hostPath:
    path: "/mnt/data"
